import matplotlib.pyplot as plt\nimport matplotlib.patches as mpatches\n\ndef generate_architecture_diagram():\n    fig, ax = plt.subplots(figsize=(10, 7))\n    ax.set_xlim(0, 10)\n    ax.set_ylim(0, 7)\n    ax.axis('off')\n\n    # Define elements of the architecture\n    data_sources = mpatches.Rectangle((1, 6), 3, 1, color='lightblue', label='Data Sources')\n    data_storage = mpatches.Rectangle((1, 4), 3, 1, color='lightgreen', label='Data Storage')\n    data_quality_checks = mpatches.Rectangle((1, 2), 3, 1, color='salmon', label='Data Quality Checks')\n    reporting_tools = mpatches.Rectangle((5, 6), 3, 1, color='lightblue', label='Reporting Tools')\n    analytics = mpatches.Rectangle((5, 4), 3, 1, color='lightgreen', label='Analytics')\n\n    # Add elements to the plot\n    ax.add_patch(data_sources)\n    ax.add_patch(data_storage)\n    ax.add_patch(data_quality_checks)\n    ax.add_patch(reporting_tools)\n    ax.add_patch(analytics)\n\n    # Add arrows\n    ax.annotate('', xy=(5, 6.5), xytext=(3, 6), arrowprops=dict(arrowstyle='->'))\n    ax.annotate('', xy=(5, 4.5), xytext=(3, 4), arrowprops=dict(arrowstyle='->'))\n    ax.annotate('', xy=(5, 2.5), xytext=(3, 2), arrowprops=dict(arrowstyle='->'))\n\n    # Add labels\n    ax.text(2.5, 6.5, 'Data Sources', ha='center', fontsize=12)\n    ax.text(2.5, 4.5, 'Data Storage', ha='center', fontsize=12)\n    ax.text(2.5, 2.5, 'Data Quality Checks', ha='center', fontsize=12)\n    ax.text(6.5, 6.5, 'Reporting Tools', ha='center', fontsize=12)\n    ax.text(6.5, 4.5, 'Analytics', ha='center', fontsize=12)\n\n    # Save the diagram as PNG file\n    plt.title('Healthcare Data Quality Workflow', fontsize=16)\n    plt.savefig('healthcare_data_quality_workflow.png')\n    plt.close()\n\n# Generate the architecture diagram\ngenerate_architecture_diagram()